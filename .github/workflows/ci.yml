name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2

      - run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Test
        run: bun test

      # release は tag を --ccx-version で焼き込む。その経路が効かないと全 release が
      # package.json の版を名乗るので、同じ経路で作って確かめる
      - name: Build
        run: |
          bun run scripts/build.ts --ccx-version 0.0.0-ci
          test "$(./ccx --version)" = 0.0.0-ci

      # 単一バイナリ固有の経路 (埋めた asset → cache → dlopen) は bun test では通らない
      - name: Smoke the compiled binary
        run: scripts/smoke.sh ./ccx

  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./...

  proto:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: bufbuild/buf-action@v1
        with:
          setup_only: true

      - name: Lint
        run: buf lint packages/proto

      # 生成物は commit されている。手で直したもの、あるいは proto を変えて生成し
      # 忘れたものは、ここで落とす。Go と TS の両方を見るのは、片方だけ再生成して
      # もう片方が古いまま緑になる形が、いちばん見つけにくいため。
      #
      # intent-to-add を挟むのは、git diff が追跡下のファイルしか見ないから。proto を
      # 足して生成物が新規ファイルになった場合、それを commit し忘れても未追跡のまま
      # diff に出ず、gate が緑で通る (この PR で実際にその状態になっていた)。
      - name: Generated code is in sync with the schema (Go)
        run: |
          buf generate packages/proto --template packages/proto/buf.gen.yaml -o packages/proto
          git add --intent-to-add packages/proto/gen
          git diff --exit-code -- packages/proto/gen

      - name: Generated code is in sync with the schema (TS)
        run: |
          buf generate packages/proto --template packages/proto/buf.gen.ts.yaml -o packages/proto
          git add --intent-to-add packages/proto/gen
          git diff --exit-code -- packages/proto/gen
