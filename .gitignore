node_modules/
dist/

# DuckDB assets fetched by scripts/duckdb-assets.ts (postinstall)
/.build/

# compiled binary (bun build --compile)
/ccx
/ccx-*

# go build outputs (e.g. `go build ./apps/agent` drops `agent` at the root)
/agent
/ccxd
/apps/*/agent
